spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/sflbilling
    username: postgres
    password: postgres
    hikari:
      auto-commit: false
  jpa:
    database-platform: bo.com.luminia.sflbilling.msreport.config.dbplatform.FixedPostgreSQL10Dialect
